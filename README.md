# Dependencies
### For whatever reason, I had to run the following as root (not sudo):
 ```
 sudo apt-get install npm
 ```

# Run 

  ```
  cd mean-master
  sudo npm install
  sudo bower install
  sudo npm start
  ```
